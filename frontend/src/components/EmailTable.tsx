import type { Email } from '../types/email'; import { StatusBadge } from './StatusBadge';
export function EmailTable({items,loading}:{items:Email[];loading:boolean}){if(loading)return <div className="card p-5 animate-pulse text-slate-400">Loading email activity…</div>;if(!items.length)return <div className="card p-12 text-center text-slate-500"><div className="text-3xl">✉️</div><p className="mt-3">No emails here yet. Compose a campaign to get started.</p></div>;return <div className="card overflow-x-auto"><table className="w-full text-left text-sm"><thead className="border-b bg-slate-50 text-xs uppercase text-slate-500"><tr><th className="p-4">Recipient</th><th>Subject</th><th>Scheduled</th><th>Status</th></tr></thead><tbody>{items.map(e=><tr key={e.id} className="border-b last:border-0"><td className="p-4 font-medium">{e.recipient}</td><td className="max-w-xs truncate">{e.subject}</td><td>{new Date(e.scheduledAt).toLocaleString()}</td><td><StatusBadge status={e.status}/></td></tr>)}</tbody></table></div>}
