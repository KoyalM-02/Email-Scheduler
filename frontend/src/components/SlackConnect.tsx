'use client'; import { useEffect,useState } from 'react'; import { api } from '../lib/api';
export function SlackConnect(){const [connected,setConnected]=useState(false);useEffect(()=>{api.me().then(x=>setConnected(x.slackConnected)).catch(()=>undefined)},[]);const toggle=async()=>{try{if(connected){await api.disconnectSlack();setConnected(false)}else await api.slackStart()}catch(e){alert(e instanceof Error?e.message:'Could not update Slack connection')}};return <button onClick={toggle} className="rounded-lg border border-slate-300 px-3 py-2 text-sm font-semibold">{connected?'Disconnect Slack':'Connect Slack'}</button>}
